# utils

Supporting helpers for `follow_goblin_4`.

Use this subtree for session-state helpers, logging setup, and other narrow
support code that should stay outside the network and visualization layers.
